function result = backscatter_angles(x_pos,y_pos,z_pos,path_phi,path_theta,x_range,y_range,z_range);
  if(or(x_pos < 0), (x_pos) > x_range),
  
  
else
  if(or(y_pos < 0), y_pos > y_range)),
else 
  
endfunction
